[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd backend && npm install"]

[phases.build]
cmds = ["cd backend && node src/scripts/prebuild-seed.js || true"]

[start]
cmd = "cd backend && node src/server.js"
