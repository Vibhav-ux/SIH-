{
  "name": "mplad-sentinel",
  "version": "1.0.0",
  "scripts": {
    "start": "cd backend && node src/server.js",
    "install-backend": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
